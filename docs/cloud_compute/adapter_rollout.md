# Adapter rollout

Stage A: certify isolated adapter foundation on PR.

Stage B: wire current control-plane worker/dispatcher to the adapter while preserving exact-job atomic claim, input materialization, artifact persistence, and zero-cost policy; add regression tests.

Stage C: construct IR11 infrastructure-only replacement SHA and certify it.

Stage D: redispatch PMOD using exact historical research SHA through certified infrastructure; redispatch IR11 using its certified replacement SHA.

Original failed SHAs/runs remain immutable provenance evidence.
