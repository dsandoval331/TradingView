name: Scheduled Actions Canary

on:
  schedule:
    - cron: '7,17,27,37,47,57 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm scheduled Actions invocation
        run: echo "SCHEDULED_ACTIONS_CANARY_OK"
